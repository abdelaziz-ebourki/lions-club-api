# Data Model: OpenAPI Configuration

No new data entities are introduced by this feature.

This feature is purely configuration (adding SpringDoc OpenAPI metadata and annotations). No database tables, Flyway migrations, or domain model changes are required.
